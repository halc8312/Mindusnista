# 実機試験の記録

M1-04（2026-09-10）: 生産品の搬出・複数完了・容量超過在庫の保存を更新。**実機未確認**。
ドリル→ベルト／分配器、詰まりと解除、混在在庫、手動保存・再起動後の続行を確認する。
容量超過在庫を持つ新版保存は旧スクリプトでは読めないため、更新前の保存を退避する。
PCの回帰・抽出比較・大量完了分の算術検査を実機成功やFPSへ転記しない。

M1-03（2026-09-10）: ドリルの定期搬出の品目・相手選択を更新。**実機未確認**。
ドリル→ベルト／分配器、搬出先が詰まった時と解除後、手動保存・再起動の続行を確認する。
PCの245本と抽出比較は、実機の輸送・操作・FPS成功には転記しない。

M1-02（2026-09-10）: キャッシュと同一tickの複数搬入、保存再開を更新。**実機未確認**。
直列・両側からの流入・停止と解除・回転後の輸送・手動保存と再起動を確認する。
旧保存の読込を維持するが、新版保存は旧スクリプトでは読めないため更新前の保存を退避する。
PCの211本成功や抽出比較を、この実機欄へ成功として転記しない。

M1-01（2026-09-10）: コンベア引渡し直後の後続品の移動順を修正。実機未確認。
単体 `.py` 更新後、UI-01 の操作に加えて、直列・曲がり・詰まり解除・保存再開を確認する。
抽出 Java との比較や PC の成功は、実機の輸送・FPS・操作成功へ転記しない。

2026-09-09 M0-01: 本体無変更の移入と PC 検査を実施。新しい実機報告はありません。
以下の実機判定を維持し、PC の117本・self-test 成功を実機成功へ転記していません。
今回の詳細: [M0-01 検証記録](docs/M0_01_VERIFICATION_ja.md)。

M0-02: 配布した `.py` のバイト一致と PC self-test を検査する仕組みを追加しました。
当時の生成ファイルも同じ 0.1.2-dev で、新しい実機結果はありませんでした。

M0-03: 当時の編集元の分割後も配布 `.py` は以前とバイト一致しました。PC の隔離 import / self-test と
起動・保存・操作の模擬回帰を検査しました。この作業時点で実機報告の追加はありませんでした。

UI-01（2026-09-09）: ユーザーから、設備を選んですぐ向きを変えられる点とスライドによる連続設置は
よいとの所感、および誤スライドで置いてしまうことがあるとの報告がありました。
報告時のゲーム版・端末機種・iOS・Pythonista の版は未確認で、0.1.2 の総合実機成功とは扱いません。
この報告を受けてマス操作と静止保持による連続操作を追加しました。今回変更の実機試験は未確認です。
PC 側の検査結果は [UI-01 検証記録](docs/UI_01_VERIFICATION_ja.md) に記録します。

| 版 | 確認主体 | 確認できた内容 | 未確認 |
|---|---|---|---|
| 0.1.1-dev | ユーザーからの報告、2026-09-09 | 「起動時エラー出なかったよ！」。回転操作が不便との指摘あり。 | 機種・iOS・Pythonistaの版、FPS、保存の全経路 |
| 版未確認（UI-01 着手前の操作報告） | ユーザーからの報告、2026-09-09 | 設備選択時の方向変更とスライド連続設置への肯定的な所感。誤スライドによる意図しない設置の指摘。 | ゲーム版、機種・iOS・Pythonistaの版、再現条件、起動・保存等を含む総合試験 |
| 0.1.2-dev / UI-01 | 今回変更の実機試験は未確認 | PC側の結果は別記録 | 実際の起動、方向・マス操作、長押しと誤スライド判定、2本指の混在入力、縦横回転、表示、保存・復帰、負荷 |

0.1.2-dev / UI-01 の実機確認手順:

1. 旧スクリプトと保存を退避し、同じ場所に新版を配置。DEV 0.1.2表示と既存ゲームの復元を確認。
2. ベルト選択→マス操作 OFF の ←↑↓→ で各方向に1個ずつタップ配置。↶↷の回転方向も確認。
3. 移動/調査→荷物が載ったベルトを選択→回転。HP・荷物・銅が失われないことを確認。
4. 既存ベルトへのタップ変更と、ドラッグ通過で変更されないことを区別して確認。
5. 地面を押したまま別指で方向変更し、指を離す。意図せずピンチや二重設置にならないか確認。
6. ベルト選択→「マス操作」ON。矢印が候補だけを1マスずつ動かし、地面のタップ・スライドでも設置せず、「置く」で確定することを確認。↶で向きを変更し、ドリル・壁でも位置を調整する。
7. マス操作 OFF で素早くスライドし、ベルト・壁が置かれず、撤去もされないことを確認。少し指が揺れた普通のタップでは1回だけ操作できるかも確認。
8. 同じ場所を0.35秒ほど保持して準備表示を確認後、スライドで連続設置・撤去。途中で2本指にすると以後の操作が止まることを確認。すでに確定した分は取り消されない仕様。
9. 縦横で下部3段・16ボタンの押しやすさ、設備選択中の「メニュー → ウェーブ」、メニュー→表示・速度、ピンチを確認。手動保存後に閉じて再開。

結果と機種・OS・Pythonistaの版を追記する。模擬テストの結果をこの実機欄へ転記しない。

## ARCH-01 で追加した性能・品質・共有の試験計画

2026-09-10 時点ではすべて未実施。試験器・codec・描画試作ができた順に実機で確認する。

| 対象 | 実機で必要な確認 | 結果 |
|---|---|---|
| P-01 能力と並列性 | Python/NumPy版、API可用性、同じ総仕事量の1/2/4ワーカー、結果一致と変換・同期込み時間 | 未実施 |
| 超大規模工場・戦闘 | 規模と条件を固定したtick時間・FPS・入力遅延・ピークメモリ、持続負荷、保存停止 | 未実施 |
| R-01 効果品質 | 原作との同条件画像/動画、効果の種類・寿命・重なり・発光と描画負荷 | 未実施 |
| 原作セーブ共有 | 同じ実アプリ版のiOS/Steam/Pythonistaで単独msavとキャンペーン全体の往復・続行 | 未実施 |

詳細は [完全移植の要件](docs/FULL_PORT_REQUIREMENTS_ja.md) と各調査資料を参照。
端末・版を記録し、機能・規模・エフェクトを削って合格にしない。現行の暫定上限は残っている。

P-01 比較器を用意済み: `tools/pythonista_parallel_probe.py` 一つをPythonistaで実行する。
最初は既定の20,000件で起動と全件一致を確認し、機種・iOS・Pythonista/NumPy版と結果JSONを記録する。
今回のPC実行をこの表の実機結果には転記しない。ゲーム本体を置き換えるファイルではない。

P-02（2026-09-10）: 同じ比較器に `--resident` を指定すると、毎回変化する入力と配列保持の比較を行う。
まず20,000件で全件一致・初回・区間時間・p50/p95を記録する。[手順](docs/P_02_PROBE_ja.md)。
入力更新・配列化・計算同期・結果反映を含む時間を見て、計算部分だけを高速化成功としない。
今回のPC測定と225本の統合検査は実機未実施の欄を変更しない。持続負荷・発熱・実ピークメモリも未確認。

P-03（2026-09-10）: `tools/pythonista_array_state_probe.py`を別に用意。まず引数なし20,000件で起動し、
全件一致・保存復元後の一致・機種/iOS/Pythonista/NumPy版を記録する。[条件と手順](docs/P_03_PROBE_ja.md)。
実機は未実施。短いモデルのPC時間をFPSへ換算せず、初期取込・保存復元・持続時間・メモリも別々に確認する。
ゲーム本体を置き換えるファイルではなく、原作msavや既存ゲーム保存は扱わない。
