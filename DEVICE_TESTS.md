# 実機試験の記録

2026-09-09 M0-01: 本体無変更の移入と PC 検査を実施。新しい実機報告はありません。
以下の実機判定を維持し、PC の117本・self-test 成功を実機成功へ転記していません。
今回の詳細: [M0-01 検証記録](docs/M0_01_VERIFICATION_ja.md)。

M0-02: 配布した `.py` のバイト一致と PC self-test を検査する仕組みを追加しました。
生成ファイルも同じ 0.1.2-dev です。新しい実機結果はなく、以下の判定を維持します。

M0-03: 編集元の分割後も配布 `.py` は以前とバイト一致します。PC の隔離 import / self-test と
起動・保存・操作の模擬回帰を検査しました。実機報告の追加はなく、以下の判定を維持します。

| 版 | 確認主体 | 確認できた内容 | 未確認 |
|---|---|---|---|
| 0.1.1-dev | ユーザーからの報告、2026-09-09 | 「起動時エラー出なかったよ！」。回転操作が不便との指摘あり。 | 機種・iOS・Pythonistaの版、FPS、保存の全経路 |
| 0.1.2-dev | 未実施 | PCの自動テストのみ | 実際の起動、方向ボタン、2本指の混在入力、縦横回転、表示、保存・復帰、負荷 |

0.1.2の実機確認手順:

1. 旧スクリプトと保存を退避し、同じ場所に新版を配置。DEV 0.1.2表示と既存ゲームの復元を確認。
2. ベルト選択→←↑↓→の各方向に1個ずつ配置。↶↷の回転方向も確認。
3. 移動/調査→荷物が載ったベルトを選択→回転。HP・荷物・銅が失われないことを確認。
4. 既存ベルトへのタップ変更と、ドラッグ通過で変更されないことを区別して確認。
5. 地面を押したまま別指で方向変更し、指を離す。意図せずピンチや二重設置にならないか確認。
6. 縦横回転、メニュー→表示・速度、ピンチを確認。手動保存後に閉じて再開。

結果と機種・OS・Pythonistaの版を追記する。模擬テストの結果をこの実機欄へ転記しない。
