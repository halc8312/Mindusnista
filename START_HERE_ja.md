# iPhone の Work に渡す手順

## 渡すもの

`Mindusnista_Work_Handoff_20260909.zip` を Work の新しい作業に添付し、
`prompts/01_START_WORK_ja.txt` の全文を最初のメッセージに貼り付けます。
添付 ZIP は本体・テスト・資料を含む開発一式です。**実行用 .py だけではなく ZIP を渡してください。**
Work の画面名・モデル選択肢・利用可能なツールは、アカウントと環境で実際に確認します。
特定のモデルが選べることや、GitHub 書き込みが使えることをこの資料は保証しません。

GitHub への接続設定では `halc8312/Mindusnista` がアクセス対象か確認します。
認証情報やトークンをチャット本文に貼る必要はありません。

## 初回の合格条件

Work がソースを読み、117 本の基準テストとセルフテストを再実行し、
書き込み可能なら作業ブランチと PR を作成して、**実在するコミット ID・PR URL**を返すところまでです。
初回はゲームに機能を足しません。まず同じソースを継続開発できる状態にします。
main へのマージ・強制 push・リポジトリ設定変更は初回の依頼に含めません。

「ZIP を読み込んだ」「ファイルを作った」「リポジトリ権限に push とある」だけでは、GitHub 保存完了ではありません。

## GitHub で展開する位置

ZIP の最上位には `Mindusnista/` フォルダーがあります。
**その中身**をリポジトリ直下に入れます。

正しい例: `AGENTS.md`、`mindustry_pythonista.py`、`tests/test_engine.py`。
誤った例: `Mindusnista/AGENTS.md` をリポジトリのさらに下に入れる。
`.github/` と `.gitignore` のようなドットで始まるファイルも含めて移します。
ZIP ファイルそのものを GitHub に置くだけでは、ソースの階層として使えません。

## 書き込み機能がない場合

この引き継ぎを作った会話では、GitHub の読み取りはできましたが、ファイル更新・push・PR 作成の
アクションは提供されていませんでした。リポジトリは今回変更していません。
前の会話にあった「書き込みツールを確認した」という説明は、その能力を裏付けるものとして使わないでください。

OpenAI の通常の GitHub 接続ヘルプも読み取り専用と説明しています。
Work で別の書き込み可能な連携や認証済み開発環境を利用できるか、最初に調べます。
使えなければ実装・テスト・ZIP 出力まで進め、GitHub 未保存と明記します。
保存まで行うには書き込み対応の Codex 等の環境を設定する必要があり、プロンプトだけでは権限は増えません。

参照（2026-09-09確認）:
https://help.openai.com/en/articles/11145903-connecting-github-to-chatgpt
https://developers.openai.com/codex/cloud/

## 次からの依頼

初回 PR の main への取り込みを確認した後、`prompts/02_CONTINUE_WORK_ja.txt` を使います。
PR が未取り込みなら、先にその状況を読み取らせ、同じ作業を別ブランチで二重実装しないようにします。
ユーザーが途中から再開を明示した場合のみ、対応する作業ブランチを基準にします。

iPhone の不具合は `prompts/03_DEVICE_BUG_ja.txt`、実機結果の報告は
`docs/templates/DEVICE_RESULT_ja.md` を使います。機種・版・結果は不明なら不明のままで構いません。

## この ZIP の役割

本体は 0.1.2-dev のままです。0.1.3 や完全版が新たに実装されたわけではありません。
既存の本体、テスト、Java 参照、ライセンスを維持し、引き継ぎ資料と CI 設定を追加しています。
PC は一式を渡すための必須条件ではありません。実機動作の確認先は引き続き Pythonista です。
