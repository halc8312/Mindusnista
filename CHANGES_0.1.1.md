# 0.1.1-dev — 起動時の未初期化エラー対策

作成日：2026-09-09。対象：Pythonista用部分移植開発版。ゲーム機能の追加や完全移植ではありません。

## 報告されたエラー

```text
AttributeError: 'MindustryScene' object has no attribute '_failed'
```

0.1.0の `setup()` は、背景色の設定、保存先の取得、フォルダーの作成を済ませてから
`self._failed = False` を実行していました。その前で失敗した場合、後続の `update()` は
本来の例外ではなく `_failed` の未定義エラーを出します。

旧版では「setup前にupdateを呼ぶ場合」と「setup冒頭の保存先取得で例外を発生させ、
その後にupdateを呼ぶ場合」の両方で、報告と同じAttributeErrorを模擬環境で再現しました。
**どちらが実機で起きたか、またその最初の例外が何かは、今回のTracebackだけでは確定しません。**

## 修正内容

- `_failed` と `_ready` 等をインスタンス生成時に初期化。親の初期化中にもフラグを参照可能にしました。
- 初期化を一度だけ実行し、再入を防止。未初期化・初期化途中・失敗状態ではゲーム更新、
  タッチ操作、保存、休止・復帰処理を実行しません。
- 有効な画面サイズでsetupが未実行なら、最初のupdateから一度初期化を試みます。
  失敗を毎フレーム再試行したり、単に黒画面のままエラーを捨てたりはしません。
- 起動・更新・リサイズの最初の例外をコンソールに記録し、可能なら `crash.log` に保存。
  ログ保存やエラー画面自体が失敗しても、元のTracebackを保持します。
- スクリプトのディレクトリーを実行開始時に固定し、後続のコールバックで毎回 `__file__` を参照しません。
- 元の保存フォルダーに書き込めない場合、Pythonista側の `~/Documents/_mindustry_pythonista` を試し、
  切替を通知します。元の保存ファイルは削除も移動もしません。実際に選んだ保存先をコンソールに表示します。
- ゲーム内の版表示を0.1.1に変更。Worldクラス、ゲーム計算、保存形式のバージョンは変更していません。

## 差し替え

ゲーム画面を閉じて停止し、同じフォルダーの旧 `mindustry_pythonista.py` を新しいファイルに差し替えて、
Pythonista本体のエディタから ▶ で実行します。`_mindustry_pythonista` は削除しないでください。
画面のバージョンが **DEV 0.1.1** になれば新しいコードが動いています。

保存先の代替先への切替は、セーブデータの自動移行ではありません。元の保存を読みたい場合、
元のフォルダーが利用できる状態にするか、ゲームを停止して既存ファイルをバックアップしたうえで
`autosave.json` / `manual.json` 等を希望する保存フォルダーへコピーします。
代替先に進行状況を保存した後に元の場所が書込可能になると、次回起動は元の場所を優先します。
保存を継続するときは、コンソールの切替メッセージと保存先に注意してください。

起動に失敗したら、×でゲーム画面を閉じ、コンソールの `Mindustry Pythonista 0.1.1-dev` から
最初のTraceback全文を確認してください。ログを書ける場合は保存先の `crash.log` にも残ります。

## 検証

既存68本＋新規21本、合計 **89本の自動テストが成功**。組込セルフテストとPython 3.10構文検査も成功。
テスト条件は `TEST_REPORT.md`、実行ログは `test-results.txt` に収録。
**Linux上の模擬scene/uiでの検証です。iPhone実機、実際のPythonistaのコールバック順、描画、
File Providerの権限動作は未検証です。実機での初期原因が解消したと保証するものではありません。**
