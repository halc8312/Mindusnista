> Work + GitHub への今回の引き継ぎは `START_HERE_ja.md` と `docs/HANDOFF_ja.md` を優先してください。
> 以下は 0.1.2 に同梱された旧チャット／PC向け案内です。

# チャットから継続する／PC・Codexへ引き継ぐ

## 方針

ここまでの実装を捨てて作り直す必要はありません。このフォルダーが現在の引き継ぎ単位です。
チャットでの設計・実装・テストを続けることも、同じソースをPCの作業フォルダーに置いて育てることもできます。
長期開発ではソース、テスト、台帳、履歴を一緒に保持し、最新版を一つに決める方針を勧めます。

このZIPをPCへ展開しただけでCodexが起動・作業を開始するわけではありません。
このチャットからユーザーPCやiPhoneを操作・同期したわけでもありません。

## PCへ置く場合

ZIPを展開し、`mindustry_pythonista_dev012` フォルダーを継続開発用の名前で保存します。
まずPython 3.10以降を用意し、フォルダー内で検査します。PCにPythonistaは不要です。

```sh
python tools/check_project.py
```

Gitを導入済みなら、このフォルダーで履歴の初期状態を保存できます。
既存のGit管理フォルダーへ追加する場合は二重に初期化する必要はありません。
個人のセーブを入れないことを確認してから実行します。

```sh
git init
git add .
git commit -m "Baseline Pythonista port 0.1.2"
```

Codexを導入済みなら、このプロジェクトを開くか、フォルダー内からCLIを開始します。
公式ガイド（参照日 2026-09-09）:
- https://developers.openai.com/codex/cli/
- https://developers.openai.com/codex/guides/agents-md/

AGENTS.mdには実行環境、検査コマンド、互換性と保存の注意点を収録済みです。
最初の依頼文は次を使えます。最初から巨大な全移植を一度に書き換えさせず、現在地を確認します。

> このフォルダーのMindustry Pythonista移植を継続してください。
> AGENTS.md、PORT_STATUS.md、DEVICE_TESTS.mdを読み、check_project.pyを実行してください。
> WebViewや別言語版に置き換えず、Python本体の移植を続けます。
> まず現状と原作v159.7の差分を整理し、次の一つのサブシステムについて、
> 対象ソース・完了条件・比較テストを持つ小さな実装計画を作ってください。
> 修正時には旧セーブ、117本の基準テスト、単体.py配布、0.1.1の起動ガードを維持してください。
> 実機未確認と原作未互換を明記し、シミュレーションの未移植を見た目だけで完成扱いしないでください。

## 今後の開発順の提案

### 1. 実機操作と開発基盤

0.1.2の回転・混在タッチ・保存・復帰を実機確認します。次に、PCで育てる場合は
コア／表示／コンテンツ／保存を段階的にモジュール分割し、単一.pyへまとめるツールと同一性テストを追加します。
分割とゲームロジック変更を同じ作業に混ぜません。

### 2. 輸送・採掘の原作比較を統合単位へ拡張

今のJava fixtureは抽出した式の比較です。原作側の複数ブロックを含む比較ケースを用意し、
隣接順、更新順、minitem/mid/lastInserted、offload/dump、同時流入、詰まりを追います。
完了条件は毎tickの在庫・位置・出力の比較と、説明可能な差分の記録です。

### 3. コンテンツ登録・原作ユニット・電力・液体・製造

各追加システムに固定ID、保存の継続性、資源収支、負荷測定を設けます。
原作ユニットや戦闘へ置き換え、プレイヤー操作・建築計画も独立して進めます。

### 4. キャンペーン・研究・ロジック・原作ファイル

基盤がそろった機能から追加します。元のセーブや設計図に必要な構造は先に調べます。
通信とMOD互換は別の検査項目で、後から単純に付け足せると仮定しません。

### 5. 完成判定

対象原作バージョンに対するコンテンツ・挙動・ファイル・通信・MODの対応表と、
実機の操作・保存・負荷検証を判定資料にします。「遊べる版」と「完全互換版」を分けて表記します。
全Java/JS MOD互換や大規模マップの実用速度は、この段階では保証できていません。

## 分担と更新の受け渡し

チャットでは、このZIPの最新版を基準に実装差分とテスト結果を受け渡します。
PCを使う場合も、仕様相談やコードレビューをチャットで行い、実装本体を同じフォルダーへ反映できます。
両方で同じファイルを同時に別々に変更すると混乱するため、常に最新版を明示します。
iPhone確認はユーザーからの実行結果を記録します。未実施の作業を実施済みにしません。
