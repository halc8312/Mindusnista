# 0.1.2-dev テスト報告

実施日: 2026-09-09。対象: 同梱の `mindustry_pythonista.py`。

## 結果

**117本のunittestが成功。旧89本＋方向操作の新規28本です。**
旧テストは専用操作列に合わせて「ボタン数15」を「16」へ更新した1箇所を除き維持しました。
組込セルフテスト、PC検査スクリプトも成功しています。

全テストの出力は `test-results.txt`、セルフテストは `self-test-results.txt`、
まとめコマンドの出力は `check-project-results.txt`、環境とハッシュは `verification.json` に記録しています。

## 環境

CPython 3.13.5 / Linux。scene/uiは同梱の模擬モジュールです。
Python 3.10構文を `ast.parse(feature_version=(3, 10))` で検査しましたが、
Python 3.10の実行環境やiPhoneそのものでは試験していません。

```sh
python tools/check_project.py
python -m unittest discover -s tests -v
python mindustry_pythonista.py --self-test
```

## 新規回帰範囲

- 四方向の設置、時計／反時計回り、4回転による復元、方向ボタンだけでは建築費を消費しないこと。
- 設置前のゴーストと矢印、プレビューだけでは保存に建物が増えないこと。
- 地面を押しながら別指で回転、ドラッグ途中の方向変更、ツール変更やメニューによる入力取消。
- 選択済みベルト自身の方向を回転基準にすること、ID・HP・荷物・資源の維持。
- 既存ベルトへのタップ変更と、ドラッグ通過では方向を変えないという区別。
- 無効な回転対象、非ベルト選択、撤去された選択対象からの意図しない変更を防止。
- 方向の強調表示、ズームしても矢印の画面上の大きさを保つこと、狭い横画面での位置。
- 320×568、375×667、390×844、430×932、568×320、844×390、1024×768で方向ボタン6個が重ならず、各44ポイント以上で画面内に収まること。
- メニュー・表示／速度へのアクセス、拡大縮小・速度・コア移動、端末向き変更時の入力取消。
- 回転後の荷物入りセーブと復元、旧0.1.1メタデータ付きセーブの読込、出力先とスプライト方向、操作後300フレーム。

## 既存ロジックの保護

0.1.1と新ファイルのWorldクラスはASTで一致しました。既存のトップレベル関数／クラスで
実装を変更したものは `make_scene_class` のみです。ほかに版番号・説明文字列を更新しました。
保存schema 1、_failed/_readyの保護、保存先選択、最初の例外の保持を維持しています。

旧来の1,504ケースのJava抽出計算fixture比較は今回も既存テストで成功しました。
今回、Javaを再実行してfixtureを作り直したわけではありません。
これは原作ワールド全体・ゲームプレイ全体の比較ではありません。

## 画面・実機の限界

Pillowによる模擬HUDで縦横・小画面の文字と配置を目視確認しました。
これはSpriteKitやPythonistaのフォント・タッチを実行したものではなく、実機スクリーンショットでもありません。

**0.1.2はiPhone実機未検証です。**
0.1.1はユーザーから「起動時エラー出なかった」と報告がありましたが、
それを新版の全操作・実機速度・保存の合格として扱っていません。
詳細な実機チェック項目は `DEVICE_TESTS.md` に分けてあります。
